name: work
root: ~/code

windows:
  - code:
      root: ~/code
      panes:
        - ~/res/tmuxinator_init.sh
  - claude:
      root: ~/code
      panes:
        - source ~/res/auth.sh && claude
  - frontend:
      root: ~/code
  - storybook:
      root: ~/code
